const { registerSchema } = require('./registerSchema');
const { loginSchema } = require('./loginSchema');
const { favoriteCarSchema } = require('./favoriteCarSchema');

module.exports = {
  registerSchema,
  loginSchema,
  favoriteCarSchema,
};
