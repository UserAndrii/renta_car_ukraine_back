const { rentalCarSchema } = require('./rentalCarSchema');
const { favoriteCarSchema } = require('./favoriteCarSchema');
const { updateRentalCarSchema } = require('./updateRentalCarSchema');

module.exports = { rentalCarSchema, updateRentalCarSchema, favoriteCarSchema };
